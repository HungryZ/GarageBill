<view class="page" data-weui-theme="{{theme}}">
	<view class="content">
		<navigator class="cell" url="../{{item.url}}" wx:for="{{menu}}" wx:key="list">
			<view>{{item.text}}</view>
		</navigator>
	</view>
</view>